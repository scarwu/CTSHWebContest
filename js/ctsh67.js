'use strict'

$(document).ready(function () {
	$(window).on('resize', function () {
		$('body').css({
			'padding-top': $(window).height() + 'px'
		});
	});

	$('body').css({
			'padding-top': $(window).height() + 'px'
		});
});